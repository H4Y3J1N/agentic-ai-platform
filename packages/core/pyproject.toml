[tool.poetry]
name = "agentic-ai-core"
version = "0.1.0"
description = "Core library for Agentic AI Platform"
authors = ["Your Name <your.email@example.com>"]
packages = [{include = "agentic_ai_core"}]

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^2.0"
fastapi = "^0.100"
openai = "^1.0"
chromadb = "^0.4"
tiktoken = "^0.5"
httpx = "^0.25"
pyyaml = "^6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
